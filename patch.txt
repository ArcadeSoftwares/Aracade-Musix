--- app/src/main/java/com/arcadesoftware/musix/ui/screens/PlaylistScreen.kt
+++ app/src/main/java/com/arcadesoftware/musix/ui/screens/PlaylistScreen.kt
@@ -1390,7 +1390,8 @@
                         Row(
                             modifier = Modifier.fillMaxWidth(),
                             horizontalArrangement = Arrangement.spacedBy(16.dp)
                         ) {
                             LiquidButton(
+                                backdrop = backdrop,
                                 onClick = {
                                     if (songs.isNotEmpty()) {
@@ -1410,9 +1411,8 @@
                                 }
                             }
                             LiquidButton(
+                                backdrop = backdrop,
                                 onClick = {
                                     if (songs.isNotEmpty()) {
@@ -1421,9 +1421,7 @@
                                         PlayerManager.playQueue(shuffled, 0)
                                     }
-                                },
-                                containerColor = MaterialTheme.colorScheme.surfaceVariant,
-                                contentColor = MaterialTheme.colorScheme.onSurfaceVariant
+                                }
                             ) {
                                 Row(
@@ -1460,20 +1458,24 @@
                         )
                     } else {
-                        com.arcadesoftware.musix.components.SongRow(
-                            song = song,
-                            index = index + 1,
-                            isCurrentlyPlaying = isCurrentlyPlaying,
-                            isDownloading = isDownloading,
-                            downloadProgress = downloadProgress,
-                            onClick = {
-                                PlayerManager.currentPlayingPlaylist.value = downloadsPlaylistItem
-                                PlayerManager.playQueue(songs, index)
-                            },
-                            onMoreClick = { /* Can show options for liked song */ }
-                        )
+                        Row(modifier = Modifier.fillMaxWidth().clickable {
+                            PlayerManager.currentPlayingPlaylist.value = downloadsPlaylistItem
+                            PlayerManager.playQueue(songs, index)
+                        }.padding(horizontal = 24.dp, vertical = 10.dp), verticalAlignment = Alignment.CenterVertically) {
+                            AsyncImage(
+                                model = song.thumbnail,
+                                contentDescription = null,
+                                contentScale = ContentScale.Crop,
+                                modifier = Modifier.size(52.dp).clip(RoundedCornerShape(8.dp))
+                            )
+                            Spacer(modifier = Modifier.width(14.dp))
+                            Column(modifier = Modifier.weight(1f)) {
+                                Text(song.title, style = MaterialTheme.typography.bodyLarge, fontWeight = FontWeight.SemiBold, maxLines = 1, overflow = TextOverflow.Ellipsis)
+                                Text(song.artists.joinToString { it.name }, style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant, maxLines = 1)
+                            }
+                        }
                     }
                 }
